# Backend API Host Configuration
# This is used by the Cloud Run reverse proxy service
BACKEND_HOST=backend.com

# CORS Origin Configuration (optional)
# For production, set this to your Firebase Hosting domain
# Example: CORS_ORIGIN=https://your-app.web.app
# Default: * (allows all origins - use only for development)
# CORS_ORIGIN=*
